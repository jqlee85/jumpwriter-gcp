
export const saveWriting = (writing, pieceID=null) => {
  
  // TODO Call cloud function to save writing
  // * create piece if no id
  // * update piece if there is an id
  // * move previous version of piece to PieceHistory collection

}